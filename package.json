{
  "private": true,
  "workspaces": [
    "battleship-client",
    "battleship-server"
  ],
  "scripts": {
    "start-client": "(cd battleship-client && yarn start)",
    "start-server": "(cd battleship-server && yarn start)"
  }
}
